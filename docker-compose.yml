services:
  seq:
    image: datalust/seq:latest
    container_name: cvision-seq
    environment:
      - ACCEPT_EULA=Y
      - SEQ_FIRSTRUN_ADMINPASSWORD=Admin123!
    ports:
      - "5341:80"
      - "5342:5341"
    volumes:
      - seq-data:/data
    restart: unless-stopped

volumes:
  seq-data:
